# 预警规则配置文件（支持热更新）
flood:
  blue:
    threshold_percentile: 80
    min_duration_hours: 6
  yellow:
    threshold_percentile: 90
    min_duration_hours: 3
  orange:
    threshold_percentile: 95
    min_duration_hours: 2
  red:
    threshold_percentile: 99
    min_duration_hours: 1

drought:
  blue:
    spi_threshold: -0.5
    duration_months: 1
  yellow:
    spi_threshold: -1.0
    duration_months: 2
  orange:
    spi_threshold: -1.5
    duration_months: 3
  red:
    spi_threshold: -2.0
    duration_months: 6

general:
  lead_time_warning_hours: 6
  ensemble_trigger_prob: 0.5
